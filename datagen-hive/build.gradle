buildscript {
    ext.RxHiveVersion = "0.20.166-SNAPSHOT"
}

repositories {
    maven {
        url "http://oss.sonatype.org/content/repositories/snapshots"
    }
}

dependencies {
    compile project(":datagen-core")
    compile "com.sksamuel.rxhive:rxhive-core:$RxHiveVersion"
}